{{- $fullname := include "skythread-authentication-svc.fullname" . -}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "skythread-authentication-svc.fullname" . }}-svc
  labels:
    app: {{ include "skythread-authentication-svc.name" . }}
    chart: {{ include "skythread-authentication-svc.chart" . }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
    version: "{{ $.Values.commitSha }}"
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: {{ include "skythread-authentication-svc.fullname" . }}
  type: {{ .Values.service.type }}
