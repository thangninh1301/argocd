{{- $fullname := include "skythread-aadesh.fullname" . -}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "skythread-aadesh.fullname" . }}-svc
  labels:
    app: {{ include "skythread-aadesh.name" . }}
    chart: {{ include "skythread-aadesh.chart" . }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
    version: "{{ $.Values.commitSha }}"
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: {{ include "skythread-aadesh.fullname" . }}
  type: {{ .Values.service.type }}
